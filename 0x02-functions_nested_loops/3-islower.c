#include "main.h"

/**
 * main - Entry point
 * description: write a function that checks for lowervcase character
 *
 * Return: Always 0.
 */
int _islower(int c)
{

	if (c >= 97; && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
	return (0);
}
