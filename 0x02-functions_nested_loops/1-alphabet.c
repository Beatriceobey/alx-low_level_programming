#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0
 */
void print_alphabet(void);
{
	int lowercase;

	for (lowercase = 'a'; lowercase <= 'z'; lowercase++)
	{
		_putchar(lowercase);
	}
	_putchar('\n');
	return(0)
}
